import { IonContent, IonPage } from '@ionic/react';
import React from 'react';

const Channels = () => {
    return (
        <IonPage>
            <IonContent>
                channel
            </IonContent>
        </IonPage>
    );
};

export default Channels;
