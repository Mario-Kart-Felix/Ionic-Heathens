import { IonContent, IonPage, IonTitle } from '@ionic/react';
import React from 'react';

const NotFound = () => {
    return (
        <IonPage>
            <IonContent>
                <IonTitle>
                    404
                </IonTitle>
            </IonContent>
        </IonPage>
    );
};

export default NotFound;
