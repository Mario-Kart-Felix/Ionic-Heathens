import { IonPage, IonTitle } from '@ionic/react';
import React from 'react';

const Navbar = () => {
    return (
        <IonPage>
            <IonTitle>
                Navbar
            </IonTitle>
        </IonPage>
    );
};

export default Navbar;

