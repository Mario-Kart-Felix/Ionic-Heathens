import { IonPage, IonTitle } from '@ionic/react';
import React from 'react';

const Register = () => {
    return (
        <IonPage>
            <IonTitle>
                Register
            </IonTitle>
        </IonPage>
    );
};

export default Register;

